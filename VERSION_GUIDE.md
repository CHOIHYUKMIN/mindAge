# 버전 관리 가이드

## 버전 업데이트 방법

이제 버전을 업데이트하려면 **단 한 곳**만 수정하면 됩니다!

### 📝 수정 파일: `js/config.js`

```javascript
const CONFIG = {
    // ... 기타 설정 ...
    
    // 버전 관리 (한 곳에서 관리)
    VERSION: '2.3.1',              // ← 여기만 수정!
    VERSION_NAME: 'Christmas Edition 🎄'  // ← 버전 이름도 여기서!
};
```

### ✅ 자동 적용되는 곳

1. **모든 JavaScript 파일** (`?v=버전` 쿼리 파라미터)
   - `js/i18n/ko.js`
   - `js/i18n/en.js`
   - `js/i18n/zh.js`
   - `js/questions.js`
   - `js/ageDetection.js`
   - `js/share.js`
   - `js/i18n.js`
   - `js/snow.js`
   - `js/app.js`

2. **모든 CSS 파일**
   - `css/style.css`
   - `css/animations.css`

3. **버전 푸터**
   - 메인 페이지 하단에 표시되는 버전 정보

### 🚀 배포 프로세스

1. `js/config.js`에서 `VERSION` 수정
2. Git commit & push
3. 끝! 🎉

모든 파일이 자동으로 새 버전으로 캐시 무효화됩니다.

### 📌 주의사항

- `index.html`, `privacy.html`, `terms.html`의 `<head>` 부분에 있는 임시 CONFIG 정의도 동일한 버전으로 유지해야 합니다.
- 메이저 업데이트 시: `2.x.x` → `3.0.0`
- 마이너 업데이트 시: `2.3.x` → `2.4.0`
- 패치 업데이트 시: `2.3.1` → `2.3.2`
